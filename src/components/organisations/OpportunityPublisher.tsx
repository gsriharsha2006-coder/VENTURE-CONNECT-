"use client";

import { useMemo, useState } from "react";
import { CheckCircle2, LoaderCircle } from "lucide-react";
import { Button } from "@/components/ui/Button";
import { Card, CardHeader } from "@/components/ui/Card";
import { StatusMessage } from "@/components/ui/FeedbackState";

export function OpportunityPublisher({ role }: { role: string }) {
  const organiser = role === "hackathon_organizer" || role === "event_organizer";
  const [values, setValues] = useState({ title: "", opportunity_type: organiser ? "Hackathon" : role === "investor" ? "Investor opportunity" : "Incubator program", guidelines: "", eligibility: "", deadline: "", mode: "Remote", location: "", application_method: organiser ? "internal_registration" : "idea_workspace_application", external_link: "", contact_email: "", event_start_date: "", event_end_date: "", team_size: "", prize_or_funding: "" });
  const [busy, setBusy] = useState(false); const [error, setError] = useState(""); const [published, setPublished] = useState("");
  const types = useMemo(() => organiser ? ["Hackathon", "Competitions", "Company challenge/debug challenge", "Workshop", "Startup event"] : role === "investor" ? ["Investor opportunity"] : ["Incubator program", "Accelerator program"], [organiser, role]);
  function set(key: string, value: string) { setValues((current) => ({ ...current, [key]: value })); }
  async function submit(event: React.FormEvent) { event.preventDefault(); setBusy(true); setError(""); setPublished(""); try { const response = await fetch("/api/opportunities", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(values) }); const payload = await response.json() as { data?: { id: string; title: string }; error?: string }; if (!response.ok || !payload.data) throw new Error(payload.error ?? "Opportunity could not be published."); setPublished(`${payload.data.title} is published and available to eligible founders.`); } catch (reason) { setError(reason instanceof Error ? reason.message : "Opportunity could not be published."); } finally { setBusy(false); } }
  return <Card><CardHeader eyebrow={organiser ? "Organiser publishing" : "Organisation publishing"} title="Create an opportunity" />{error ? <StatusMessage tone="error" className="mb-4">{error}</StatusMessage> : null}{published ? <StatusMessage tone="success" className="mb-4"><CheckCircle2 size={16} />{published}</StatusMessage> : null}<form onSubmit={submit} className="grid gap-4 md:grid-cols-2"><label className="md:col-span-2"><span className="text-sm font-semibold">Opportunity title</span><input required value={values.title} onChange={(event) => set("title", event.target.value)} className="mt-2 h-11 w-full rounded-lg border border-slate-300 px-3" /></label><label><span className="text-sm font-semibold">Opportunity type</span><select value={values.opportunity_type} onChange={(event) => set("opportunity_type", event.target.value)} className="mt-2 h-11 w-full rounded-lg border border-slate-300 px-3">{types.map((type) => <option key={type}>{type}</option>)}</select></label><label><span className="text-sm font-semibold">Application method</span><select disabled={!organiser} value={values.application_method} onChange={(event) => set("application_method", event.target.value)} className="mt-2 h-11 w-full rounded-lg border border-slate-300 px-3 disabled:bg-slate-100">{organiser ? <><option value="internal_registration">Internal Venture Connect form</option><option value="external_registration">External official registration</option></> : <option value="idea_workspace_application">Apply with Idea + Quality Check</option>}</select></label><label className="md:col-span-2"><span className="text-sm font-semibold">Description</span><textarea required rows={5} value={values.guidelines} onChange={(event) => set("guidelines", event.target.value)} className="mt-2 w-full rounded-lg border border-slate-300 px-3 py-2" /></label><label className="md:col-span-2"><span className="text-sm font-semibold">Eligibility</span><textarea rows={3} value={values.eligibility} onChange={(event) => set("eligibility", event.target.value)} className="mt-2 w-full rounded-lg border border-slate-300 px-3 py-2" /></label><label><span className="text-sm font-semibold">Deadline</span><input required type="date" value={values.deadline} onChange={(event) => set("deadline", event.target.value)} className="mt-2 h-11 w-full rounded-lg border border-slate-300 px-3" /></label><label><span className="text-sm font-semibold">Mode</span><select value={values.mode} onChange={(event) => set("mode", event.target.value)} className="mt-2 h-11 w-full rounded-lg border border-slate-300 px-3"><option>Remote</option><option>Hybrid</option><option>Offline</option></select></label><label><span className="text-sm font-semibold">Location or venue</span><input value={values.location} onChange={(event) => set("location", event.target.value)} className="mt-2 h-11 w-full rounded-lg border border-slate-300 px-3" /></label><label><span className="text-sm font-semibold">Prize, funding, or benefit</span><input value={values.prize_or_funding} onChange={(event) => set("prize_or_funding", event.target.value)} className="mt-2 h-11 w-full rounded-lg border border-slate-300 px-3" /></label>{organiser ? <><label><span className="text-sm font-semibold">Event start</span><input type="date" value={values.event_start_date} onChange={(event) => set("event_start_date", event.target.value)} className="mt-2 h-11 w-full rounded-lg border border-slate-300 px-3" /></label><label><span className="text-sm font-semibold">Event end</span><input type="date" value={values.event_end_date} onChange={(event) => set("event_end_date", event.target.value)} className="mt-2 h-11 w-full rounded-lg border border-slate-300 px-3" /></label><label><span className="text-sm font-semibold">Team-size rule</span><input value={values.team_size} onChange={(event) => set("team_size", event.target.value)} placeholder="2-4 members" className="mt-2 h-11 w-full rounded-lg border border-slate-300 px-3" /></label>{values.application_method === "external_registration" ? <label><span className="text-sm font-semibold">Official registration URL</span><input required type="url" value={values.external_link} onChange={(event) => set("external_link", event.target.value)} placeholder="https://" className="mt-2 h-11 w-full rounded-lg border border-slate-300 px-3" /></label> : null}</> : null}<label><span className="text-sm font-semibold">Contact email</span><input type="email" value={values.contact_email} onChange={(event) => set("contact_email", event.target.value)} className="mt-2 h-11 w-full rounded-lg border border-slate-300 px-3" /></label><Button type="submit" className="md:col-span-2" disabled={busy}>{busy ? <LoaderCircle className="animate-spin" size={16} /> : <CheckCircle2 size={16} />}Publish opportunity</Button></form></Card>;
}
